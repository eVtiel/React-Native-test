import React from 'react';
import { Text, View, Input, StyleSheet} from 'react-native';

const TextScreen = () => {
    return <Text>some</Text>;
}

const styles = StyleSheet.create({});

export default TextScreen;